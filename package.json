{
  "name": "reportform-zm-school-results",
  "version": "3.0.0",
  "private": true,
  "description": "Reportform ZM V3.0 production-storage build with PostgreSQL persistence, database recovery snapshots and JSON backup restore.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
