{
  "name": "reportform-zm-school-results",
  "version": "2.8.0",
  "private": true,
  "description": "ReportForm ZM V2.8 offline-first mark entry: cached teacher sheets, local outbox, automatic sync and simple connection status.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
